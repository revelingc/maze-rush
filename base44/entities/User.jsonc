{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "star_skin_owned": {
      "type": "boolean",
      "description": "Whether the user owns the Shooting Star ball skin cosmetic (granted by the payments webhook)",
      "default": false
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}