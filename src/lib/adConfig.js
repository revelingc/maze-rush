// Google Mobile Ads (AdMob) configuration for Maze Rush.
//
// The Google Mobile Ads SDK is a native iOS/Android SDK — it is initialized by
// the native app shell, not imported here. These ids are passed to the native
// layer via the `window.NativeAdMob` bridge (see AdBanner.jsx).
//
//   ADMOB_APP_ID            — app id (~ format), used to initialize the SDK.
//   ADMOB_BANNER_AD_UNIT_ID — ad unit id (/ format), used to request a banner.
//
// Replace the banner ad unit id with your own once you create a banner ad unit
// in the AdMob console. The app id below is already set.

export const ADMOB_APP_ID = "ca-app-pub-3520421213819679~1862476035";

// Banner ad unit id (/ format). Create one in AdMob and paste it here.
export const ADMOB_BANNER_AD_UNIT_ID = "ca-app-pub-3520421213819679/XXXXXXXXXX";